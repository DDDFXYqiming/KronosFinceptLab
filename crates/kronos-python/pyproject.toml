[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "kronos-fincept-native"
version = "0.1.0"
requires-python = ">=3.11"
description = "Optional Rust acceleration module for KronosFinceptLab."

[tool.maturin]
module-name = "kronos_fincept_native"
